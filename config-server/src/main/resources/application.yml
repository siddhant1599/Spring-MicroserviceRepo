spring.application.name: config-server
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/siddhant1599/Spring-config-server-ecommerceApp
          default-label: master

server:
  port: 8888


